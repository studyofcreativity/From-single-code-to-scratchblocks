function translateToScratchblocks(decodedCode, language = "en") {
  // En este ejemplo, simplemente pasamos el código ya convertido.
  // Puedes expandir para hacer más compleja la traducción si lo deseas.
  return decodedCode;
}
